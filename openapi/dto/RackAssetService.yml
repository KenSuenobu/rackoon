type: object
description: RackAssetService defines a mapping to the `opendc.rack_asset_service` table.
required:
properties:
  id:
    description: ID of the Rack Asset Service
    type: integer
    format: int32
  creatorId:
    description: ID of the User that created this object
    type: integer
    format: int32
  rackAssetId:
    description: ID of the Rack Asset that this service is a part of
    type: integer
    format: int32
    nullable: false
  name:
    description: The name of the service
    type: string
    nullable: false
  description:
    description: Detailed description of the rack service provided
    type: string
  accessUrl:
    description: The access URL of the service
    type: string
